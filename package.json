{
  "name": "whatsapp-prospect-agent",
  "version": "2.0.0",
  "description": "Agent WhatsApp — prospection, automatisations et console Green-API",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "setup": "npm install",
    "tunnel": "npx cloudflared tunnel --url http://localhost:3000"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/static": "^8.1.1",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
